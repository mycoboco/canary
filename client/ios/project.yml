name: Canary
options:
  bundleIdPrefix: org.woong.canary
  deploymentTarget:
    iOS: "26.0"
  xcodeVersion: "16.0"
  groupSortPosition: top

packages:
  CachingPlayerItem:
    url: https://github.com/sukov/CachingPlayerItem
    from: "2.2.0"

targets:
  Canary:
    type: application
    platform: iOS
    sources:
      - Canary
      - Shared
    settings:
      base:
        INFOPLIST_FILE: Canary/Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: org.woong.canary
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: 1
        SWIFT_VERSION: "6.0"
        DEVELOPMENT_TEAM: ""
        CODE_SIGN_ENTITLEMENTS: Canary/Canary.entitlements
    dependencies:
      - package: CachingPlayerItem
      - target: CanaryWidget
    info:
      path: Canary/Info.plist
      properties:
        UIBackgroundModes:
          - audio
        NSAppTransportSecurity:
          NSAllowsLocalNetworking: true
        CFBundleDisplayName: Canary
        UILaunchScreen: {}
        CFBundleURLTypes:
          - CFBundleURLName: org.woong.canary
            CFBundleURLSchemes:
              - canary

  CanaryWidget:
    type: app-extension
    platform: iOS
    sources:
      - CanaryWidget
      - Shared
    settings:
      base:
        SWIFT_VERSION: "6.0"
        PRODUCT_BUNDLE_IDENTIFIER: org.woong.canary.widget
        INFOPLIST_FILE: CanaryWidget/Info.plist
        CODE_SIGN_ENTITLEMENTS: CanaryWidget/CanaryWidget.entitlements
        DEVELOPMENT_TEAM: ""

  CanaryTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - CanaryTests
    dependencies:
      - target: Canary
    settings:
      base:
        SWIFT_VERSION: "6.0"
